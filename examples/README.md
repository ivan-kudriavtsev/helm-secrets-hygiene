# Examples

Runnable demonstrations.

- The render-time demonstration: a chart that a source-tree content scanner
  passes clean, while the rendered-output policy catches an inline `kind: Secret`.
  This is the single most important artifact here - it shows why you must scan
  rendered output, reproducibly, in seconds.
